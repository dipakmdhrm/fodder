#!/bin/bash
set -e

APT_REPO="https://dipakmdhrm.github.io/fodder"
KEYRING="/etc/apt/keyrings/fodder.gpg"
SOURCES="/etc/apt/sources.list.d/fodder.list"

# On a fresh install ($2 is empty; on upgrade it holds the old version),
# register the signed apt repository so `apt upgrade` tracks future releases.
if [ -z "$2" ]; then
    mkdir -p /etc/apt/keyrings
    if curl -fsSL "${APT_REPO}/key.gpg" | gpg --dearmor -o "$KEYRING" 2>/dev/null; then
        echo "deb [arch=amd64,arm64 signed-by=${KEYRING}] ${APT_REPO} stable main" > "$SOURCES"
        apt-get update -qq 2>/dev/null || true
    fi
fi

# Refresh the desktop database and icon cache so the launcher/icon show up.
update-desktop-database /usr/share/applications 2>/dev/null || true
gtk-update-icon-cache -f -t /usr/share/icons/hicolor 2>/dev/null || true

exit 0
